# Full notebook + experiment lock (large). For gpuserver-style minimal stack see
# requirements-experiment-server.txt in the README.
# Also: requirements-runtime.txt, requirements-notebook.txt
-r requirements-repro-lock.txt
