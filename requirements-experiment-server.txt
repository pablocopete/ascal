# Pip pin set aligned with the gpuserver `ascal_env2` conda snapshot (Python 3.10.x).
# ASCAL + Unified Planning + Fast Downward only — no Jupyter/matplotlib stack.
# Usage: pip install -r requirements-experiment-server.txt && pip install -e .
numpy==1.25.2
scipy==1.11.2
networkx==3.1
ConfigSpace==0.7.1
more-itertools==10.1.0
pyparsing==3.1.1
typing-extensions==4.7.1
unified-planning==1.3.0
up-fast-downward==0.2.3
meta-planning==1.0
